function scripts() {
  // вставляем стили для скрытия блоков
  $("body *:first").before(
    "<style>" +
      "noindex { display: none !important; width: 0px !important; height: 0px !important;}" +
      "</style>",
  );
  $().addClass;
}
